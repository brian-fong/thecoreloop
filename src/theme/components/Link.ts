const Link = {
  // 1. We can update the base styles
  baseStyle: {
    color: "rgba(255, 255, 255, 0.8)",
    transition: "all 200ms",
    textDecoration: "none",
    _hover: {
      color: "tcl_teal",
      textDecoration: "none",
    }
  },
}

export default Link;

